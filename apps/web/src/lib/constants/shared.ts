import c from "config";

export const ONE_HOUR_IN_MILLISECONDS = 3600000;
export const VERCEL_IP_TIMEZONE_HEADER_KEY = "x-vercel-ip-timezone";
export const BUCKET_RESUME_URL = `${c.hackathonName}/${c.itteration}/resume`;
